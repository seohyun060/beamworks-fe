import React from 'react';
import Publication from '../Publication';
type Props = {};

const PublicationContainer = (props: Props) => {
	return <Publication />;
};

export default PublicationContainer;
